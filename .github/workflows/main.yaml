on: 
  push:
    branches: 
      - master
name: Test, Build and Release apk
jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-java@v1
        with:
          java-version: '12.x'
      - uses: subosito/flutter-action@v1
        with:
          channel: 'stable'
        env:
          KEY_JKS: ${{ secrets.ANDROID_SIGNINKEYBASE64 }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEYSTOREPASSWORD }}
          ALIAS_PASSWORD: ${{ secrets.ANDROID_KEYPASSWORD }}
          run: echo $KEY_JKS > key.jks && flutter pub get && flutter build appbundle
      - uses: actions/upload-artifact@v2
        with:
          name: Release AAB
          path: build/app/outputs/bundle/release/*.aab
